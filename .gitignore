node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vs/
logs/

# Fotos crudas de origen para el seed de publicaciones reales (ver
# backend/scripts/real-listings-data.js). No se versionan: una vez
# subidas al bucket de Supabase Storage al correr `npm run seed`, el
# repo ya no las necesita.
IMAGENES/
